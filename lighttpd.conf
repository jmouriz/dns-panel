server.document-root = "/var/www/localhost/htdocs"
server.port = 80
#server.username = "root"
#server.groupname = "root"
server.errorlog = "/dev/stderr"
accesslog.filename = "/dev/stdout"

server.modules = (
  "mod_access",
  "mod_accesslog",
  "mod_alias",
  "mod_compress",
  "mod_redirect",
  "mod_fastcgi",
  "mod_openssl"
)

$SERVER["socket"] == ":443" {
  ssl.engine = "enable"
  ssl.pemfile = "/etc/lighttpd/certs/bootstrap.pem"
}

$SERVER["socket"] == ":80" {
  $HTTP["host"] =~ "^([^:]+)" {
    url.redirect = ( "^/(.*)" => "https://%1:8080/$1" )
  }
}

index-file.names = ( "index.php", "index.html" )
mimetype.assign = (
  ".html" => "text/html",
  ".txt"  => "text/plain",
  ".css"  => "text/css",
  ".js"   => "application/javascript",
  ".json" => "application/json",
  ".png"  => "image/png",
  ".jpg"  => "image/jpeg",
  ".jpeg" => "image/jpeg",
  ".gif"  => "image/gif",
  ".svg"  => "image/svg+xml",
  ".php"  => "text/html"
)

fastcgi.server = (
  ".php" => ((
    "socket" => "/tmp/php-fcgi.sock",
    "bin-path" => "/usr/bin/php-cgi85",
    "max-procs" => 1,
    "bin-environment" => (
      "PHP_FCGI_CHILDREN" => "4",
      "PHP_FCGI_MAX_REQUESTS" => "1000"
    )
  ))
)
